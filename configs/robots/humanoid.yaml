name: IsaacLab_MuJoCo_Humanoid
asset:
  source: isaaclab_assets.robots.humanoid.HUMANOID_CFG
  usd_path: ${ISAAC_NUCLEUS_DIR}/Robots/IsaacSim/Humanoid/humanoid_instanceable.usd
  prim_path: /World/Humanoids/Origin.*/Robot
  fix_base: false

actuators:
  strategy: implicit_pd
  joint_groups:
    legs: []
    torso: []
    arms: []
    head: []

sensors:
  imu: false
  feet_contact: false
  ball_relative_pose: false
  goal_relative_pose: false

notes:
  - This branch starts with the built-in Isaac Lab MuJoCo humanoid.
  - The first goal is visual inspection only, not locomotion or soccer.
  - Add sensors and task-specific observations after the humanoid loads reliably.
