name: TBD_HUMANOID
asset:
  usd_path: assets/robots/humanoid/TBD_HUMANOID.usd
  prim_path: /World/envs/env_.*/Robot
  fix_base: false

actuators:
  strategy: implicit_pd
  joint_groups:
    legs: []
    torso: []
    arms: []
    head: []

sensors:
  imu: true
  feet_contact: true
  ball_relative_pose: true
  goal_relative_pose: true

notes:
  - Replace TBD values after selecting the humanoid model.
  - Keep joint names consistent with the USD articulation and policy action order.
